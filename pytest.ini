[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v -k "not hybrid_ai"

# === Ajout pour neutraliser temporairement le filtre pour les tests hybrid_ai ===
# Pour lancer tous les tests, utiliser l'option : -o addopts=
